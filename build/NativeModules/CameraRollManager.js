var 
CameraRollManager={
saveImageWithTag:function saveImageWithTag(imageTag){
return Promise.resolve(['/asset/url']);},

getPhotos:function getPhotos(params){
return Promise.resolve([
// TODO(lmr):
]);}};



module.exports=CameraRollManager;